# J-Quants Premium API — paste your refresh token here in a real .env (NOT committed).
# Get it from https://jpx-jquants.com/ (My Page → refresh token).
JQUANTS_REFRESH_TOKEN=
# Optional alternative: the server can mint a refresh token from these instead.
JQUANTS_EMAIL=
JQUANTS_PASSWORD=
